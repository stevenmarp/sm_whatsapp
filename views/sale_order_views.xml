<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="view_order_form_whatsapp" model="ir.ui.view">
        <field name="name">sale.order.form.whatsapp</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button name="action_send_whatsapp" type="object"
                        string="WhatsApp" icon="fa-comments"
                        class="btn-secondary"
                        groups="sm_whatsapp.group_whatsapp_user"/>
            </xpath>
        </field>
    </record>

</odoo>
